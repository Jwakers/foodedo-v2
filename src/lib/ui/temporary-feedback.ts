/**
 * Temporary stand-in until toast / sheet feedback exists.
 * Do not expand this into a permanent product pattern.
 */
export function temporaryFeedback(message: string) {
  window.alert(message);
}
